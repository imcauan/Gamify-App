import { CommentaryContent } from "./CommentaryContent";
import { CommentaryImage } from "./CommentaryImage";
import { CommentaryRoot } from "./CommentaryRoot";

export const CommentaryCard = {
    Root: CommentaryRoot,
    Image: CommentaryImage,
    Content: CommentaryContent
}